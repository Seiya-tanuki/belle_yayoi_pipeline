# ai/packs/ (optional)

Optional storage for higher-level "packs" (work packs / execution packs).
If used, keep the source of truth under `ai/packs/` and treat `.ai/` as runtime only.

This v2.0 scaffold does not require packs to operate.
